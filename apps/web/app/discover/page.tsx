"use client";import {useEffect,useState} from "react";import {api} from "../../lib/api";import AppShell from "../app-shell";
type P={id:string;display_name:string;gender?:string;location?:string;education?:string;profession?:string;bio?:string};
export default function Discover(){const[people,setPeople]=useState<P[]>([]);const[location,setLocation]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");
async function load(){setLoading(true);try{setPeople(await api<P[]>(`/api/v1/discover${location?`?location=${encodeURIComponent(location)}`:""}`))}catch(e){setError(e instanceof Error?e.message:"Please sign in first")}finally{setLoading(false)}}
useEffect(()=>{load()},[]);
return <AppShell><main className="mx-auto max-w-7xl px-4 py-8 sm:px-6 md:py-12 lg:px-10"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="sans text-xs uppercase tracking-[.2em] text-[#a6535e]">Discover</p><h1 className="mt-2 text-4xl sm:text-5xl">People looking for something real.</h1></div><div className="flex gap-2"><input className="field max-w-xs" value={location} onChange={e=>setLocation(e.target.value)} placeholder="Filter by city"/><button onClick={load} className="rounded-2xl bg-[#231f20] px-5 font-sans text-sm text-white">Filter</button></div></div>{error&&<p className="sans mt-6 text-sm text-[#a6535e]">{error}</p>}{loading?<p className="sans mt-10 text-sm text-[#756b69]">Loading profiles…</p>:<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{people.map(p=><article key={p.id} className="rounded-[28px] border border-[#e6d8d0] bg-white p-6 shadow-sm"><div className="flex h-44 items-center justify-center rounded-[22px] bg-[#efe3d7] text-5xl">{p.display_name?.charAt(0)||"M"}</div><div className="mt-5"><div className="flex items-center justify-between"><h2 className="text-2xl">{p.display_name}</h2><span className="sans rounded-full bg-[#efe3d7] px-2.5 py-1 text-[10px]">Profile</span></div><p className="sans mt-2 text-sm text-[#756b69]">{[p.location,p.profession].filter(Boolean).join(" · ")}</p><p className="sans mt-4 line-clamp-3 text-sm leading-6 text-[#756b69]">{p.bio||"A new Mangalamm member."}</p><button className="sans mt-5 w-full rounded-2xl border border-[#d8c8c0] py-3 text-sm">View profile</button></div></article>)}</div>}</main></AppShell>}